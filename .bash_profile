#############################################################################################
# Tests whether file exists and is reachable, and if so, sources it.
if [ -r ~/.bashrc ]; then
   source ~/.bashrc
fi
